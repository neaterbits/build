package com.neaterbits.build.buildsystem.maven.parse;

import com.neaterbits.util.parse.context.Context;

final class StackScriptSourceDirectory extends StackText {

    StackScriptSourceDirectory(Context context) {
        super(context);
    }
}
