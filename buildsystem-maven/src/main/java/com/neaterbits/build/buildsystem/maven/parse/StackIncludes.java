package com.neaterbits.build.buildsystem.maven.parse;

import com.neaterbits.build.buildsystem.common.parse.StackStrings;
import com.neaterbits.util.parse.context.Context;

final class StackIncludes extends StackStrings {

    StackIncludes(Context context) {
        super(context);
    }
}
